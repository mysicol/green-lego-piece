import React from "react";
import ReactDOM from "react-dom/client";
import VeriFact from "./Components/VeriFact";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VeriFact />
  </React.StrictMode>
);
